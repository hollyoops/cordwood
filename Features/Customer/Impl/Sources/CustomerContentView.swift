import SwiftUI

struct CustomerContentView: View {
    var body: some View {
        VStack {
            Text("Customer Dummy Infomation")
              .multilineTextAlignment(.center)
              .font(.headline)
              .foregroundColor(.gray)
        }
    }
}
